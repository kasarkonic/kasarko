{
   "version": 2,
   "name": "kas_ar_ko",
   "public": true,
   "builds": [
      { "src": "server.js", "use": "@vercel/node" }
   ],
   "routes": [
      { "src": "/(.*)", "dest": "/server.js" }
   ]
}